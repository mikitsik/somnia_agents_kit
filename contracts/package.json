{
  "name": "somnia-agents-kit-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Hardhat contract layer for SomniaAgentsKit",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:callback": "hardhat run scripts/deploy_callback_receiver.js --network somniaShannon",
    "request:agent": "hardhat run scripts/request_agent.js --network somniaShannon"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.1.0",
    "dotenv": "^17.4.2",
    "ethers": "^6.15.0",
    "hardhat": "^2.28.0"
  }
}
